# Telegram notification secrets — copy to notify.env and fill in, then `chmod 600 notify.env`.
# Re-derive chat id: message the bot, then
#   curl -s "https://api.telegram.org/bot<TOKEN>/getUpdates" | jq '.result[].message.chat.id'
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
