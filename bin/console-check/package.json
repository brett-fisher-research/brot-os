{
  "name": "console-check",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Headless-browser smoke test: load URLs and report console errors / page errors / failed requests. Used to catch front-end bugs that server-side checks miss.",
  "dependencies": {
    "playwright-core": "^1"
  }
}
