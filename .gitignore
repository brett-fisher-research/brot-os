node_modules/
.next/
.DS_Store
*.log
HIVE.md

# experiments/ is its own tenant repo (the dir IS the repo) — ignore wholesale
/experiments/

# auto-generated by bin/render-caddy.sh
/Caddyfile
# runtime client-log sink (dashboard /api/clientlog appends here)
/logs/
# instance registry data (experiments-registry service will own this)
/registry.json

# plan archive + machine trackers (never deleted, never tracked)
.brot/
