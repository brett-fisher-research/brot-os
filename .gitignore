node_modules/
.next/
.DS_Store
*.log
HIVE.md

# the workspace layer: plans, initiatives, and the tenant sync manifest live in the
# .brot repo (gitignored by brot-os, its own backed-up repo). Never tracked here.
.brot/
# regenerated by `npm run setup`; the real one lives in .brot/sync.manifest.json
/sync.manifest.json

# dev-server + browser console output (/brot-dev, /brot-peek)
.logs/
